---
name: update-leak-detect-to-use-shared-classifier
status: open
created: 2026-02-09T17:09:30Z
updated: 2026-02-09T17:09:30Z
---

# Update leak_detect.py to use shared interface classifier

## Description

Replace the local `TUNNEL_INTERFACE_PREFIXES` constant and inline `startswith()` checks in `leak_detect.py` with imports from the new shared `backend/core/interfaces.py` module.

## Acceptance Criteria

- Remove `TUNNEL_INTERFACE_PREFIXES` from `leak_detect.py`
- Replace inline `iface.lower().startswith(TUNNEL_INTERFACE_PREFIXES)` in `check_webrtc()` and `check_ipv6()` with `is_tunnel_interface(iface)`
- All existing leak detection tests pass without modification

## Files to Modify

- `backend/core/leak_detect.py`
