---
name: tests-external-ip
status: open
created: 2026-02-10T12:26:56Z
updated: 2026-02-10T12:26:56Z
epic: external-ip
depends_on: [1, 2]
---

# Task 4: Tests for external IP module and API endpoint

## Description

Add tests with mocked HTTP calls:

1. **`tests/test_external_ip.py`** — Unit tests for `ExternalIPChecker`:
   - Successful lookup returns expected dict shape
   - Cache returns same data on repeated calls within TTL
   - Cache expires after TTL
   - API failure returns error dict
   - VPN masking detection: ISP matching "Proton" -> `vpn_masked: True`
   - VPN masking detection: ISP matching random ISP -> `vpn_masked: False`
   - `run()` returns `AuditResult`

2. **Update `tests/test_api.py`** — Add test for `GET /api/external-ip` endpoint:
   - Returns 200 with expected JSON fields
   - Mock the external HTTP call

3. **Update `tests/test_monitor.py`** — Verify `snapshot()` includes `external_ip` field

## Files to Create/Modify

- **Create:** `tests/test_external_ip.py`
- **Modify:** `tests/test_api.py` (add external-ip endpoint test)
- **Modify:** `tests/test_monitor.py` (verify external_ip in snapshot)

## Acceptance Criteria

- All new tests pass: `python -m pytest tests/ -v`
- No regressions in existing test suite
- External HTTP calls are mocked (no real network calls in tests)
