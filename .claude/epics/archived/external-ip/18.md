---
name: api-websocket-integration
status: open
created: 2026-02-10T12:26:56Z
updated: 2026-02-10T12:26:56Z
epic: external-ip
depends_on: [1]
---

# Task 2: REST endpoint and WebSocket integration

## Description

Integrate the `ExternalIPChecker` into the existing API and WebSocket infrastructure:

1. Add `GET /api/external-ip` endpoint to `backend/api/routes.py` following the existing pattern (try/except, JSONResponse on error, store in `_results`)
2. Add an `external_ip` field to `ConnectionMonitor.snapshot()` in `backend/core/monitor.py` by importing and calling `ExternalIPChecker().lookup()` — the cache ensures no excessive API calls despite the 1 Hz poll rate

## Files to Modify

- `backend/api/routes.py` — Add GET endpoint
- `backend/core/monitor.py` — Add `external_ip` to snapshot output

## Acceptance Criteria

- `GET /api/external-ip` returns JSON with ip, country, city, isp, vpn_masked, status
- WebSocket stream includes `external_ip` field in each message
- Existing endpoints and WebSocket behavior unchanged
